<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <persistence-unit name="RegistroDeEstudiantes">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <class>ar.unicen.exa.aldesal.model.Estudiante</class>
        <class>ar.unicen.exa.aldesal.model.Carrera</class>
        <class>ar.unicen.exa.aldesal.model.Inscripcion</class>

        <!-- Configuración JDBC para MySQL (se puede modificar el create) -->
        <properties>
            <property name="jakarta.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:mysql://localhost:3306/Entregable2?createDatabaseIfNotExist=true"/>
            <property name="jakarta.persistence.jdbc.user" value="root"/>
            <property name="jakarta.persistence.jdbc.password" value=""/>

            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQLDialect" /> <!-- le indica a Hibernate que estás usando MySQL 8, así genera consultas correctas. -->
            <property name="hibernate.hbm2ddl.auto" value="update" />    <!-- Update: agrega tablas/columnas nuevas sin borrar datos existentes. -->
            <property name="hibernate.show_sql" value="true" />   <!-- Para que las querys se muestren en consola -->
            <property name="hibernate.format_sql" value="true" />  <!-- Para que el formato de las consultas sea mas legible -->
        </properties>
    </persistence-unit>
</persistence>